#!/bin/sh

if [ $# -ne 1 ]; then
  echo "Usage: runiterm <dir>"
  exit 1
fi

WD=$1

osascript <<EOF
tell application "iTerm"
  set w to (create window with default profile)
  tell w
    tell the current session
      write text "cd \"$WD\"; clear"
    end tell
  end tell
end tell
EOF
